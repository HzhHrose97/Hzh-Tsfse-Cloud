<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hzh.common.mapper.user.HzhUserMapper">

    <!-- 开启二级缓存 -->
    <cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.hzh.common.pojo.user.HzhUser">
        <id column="id" property="id" />
        <result column="user_name" property="userName" />
        <result column="password" property="password" />
        <result column="user_description" property="userDescription" />
        <result column="status" property="status" />
        <result column="email" property="email" />
        <result column="phonenumber" property="phonenumber" />
        <result column="sex" property="sex" />
        <result column="avatar" property="avatar" />
        <result column="user_type" property="userType" />
        <result column="level" property="level" />
        <result column="salt" property="salt" />
        <result column="create_time" property="createTime" />
        <result column="update_by" property="updateBy" />
        <result column="update_time" property="updateTime" />
        <result column="del_flag" property="delFlag" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_name, password, user_description, status, email, phonenumber, sex, avatar, user_type, level, salt, create_time, update_by, update_time, del_flag
    </sql>

    <update id="updatePasswordByuserSelf">
        UPDATE hzh_user
        SET PASSWORD = #{password},
            create_time = #{date}
        WHERE
            ( email = #{email} AND phonenumber = #{phonenumber} )
    </update>

    <select id="getAllUserInfo" resultMap="BaseResultMap">
        SELECT * FROM hzh_user
    </select>
</mapper>
