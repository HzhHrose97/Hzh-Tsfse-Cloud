server:
  port: 9001
  servlet:
    context-path: /hzh-user

spring:
  application:
    name: userService
  datasource:
    url: jdbc:mysql://192.168.195.135:3306/hzh_tsfse_cloud?useOldAliasMetadataBehavior=true&useSSL=false&serverTimezone=GMT
    username: root
    password: 5q6W7e8R
    driver-class-name: com.mysql.cj.jdbc.Driver
  shardingsphere:
    sharding:
      default-database-strategy:
      tables:

  profiles:
    active: dev

  cloud:
    nacos:
      server-addr: 192.168.195.135:8848 # Nacos服务地址
      discovery:
        namespace: 5aad18f7-8ffe-4538-b7e7-3e8281a403f4 # 命名空间ID
        ephemeral: false # 是否为临时实例
      config:
        namespace: 5aad18f7-8ffe-4538-b7e7-3e8281a403f4 # 配置命名空间ID
        group: TSTSE-SERVER # 配置分组
        cluster: config # 配置集群名称
        isGlobal: false # 是否为全局配置
      watch:
        namespace: 5aad18f7-8ffe-4538-b7e7-3e8281a403f4 # 监控命名空间ID
        group: TSTSE-SERVER # 监控分组
        cluster: config # 监控集群名称
      loadbalance:
        algorithm: round_robin # 负载均衡算法，这里使用轮询算法
      heartbeat:
        enabled: true # 是否开启心跳检测功能
        interval: 30000 # 心跳检测间隔时间，单位为毫秒
        timeout: 10000 # 心跳超时时间，单位为毫秒
      logging:
        level: INFO # 日志级别，这里设置为INFO级别


  #  # 邮件发送
  #  mail:
  #    host: smtp.qq.com
  #    port: 465
  #    username: 1391210212@qq.com
  #    password: Asdfghjkl629...
  #    password: qyfdrxqdfumbibfb
  #    default-encoding: UTF-8
  #    protocol: smtps


  redis:
    # redis数据库索引(默认为0)，我们使用索引为3的数据库，避免和其他数据库冲突
    database: 0
    host: 192.168.195.135
    port: 6379
    pool:
      # 最大可用连接数（默认为8，负数表示无限）
      max-active: 8
      #最大空闲连接数（默认为8，负数表示无限）
      max-idle: 8
      # 最小空闲连接数（默认为0，该值只有为正数才有用）
      min-idle: 0
      # 从连接池中获取连接最大等待时间（默认为-1，单位为毫秒，负数表示无限）
      max-wait: -1
    # 配置Sentinel的流控规则
    #spring:
    #  cloud:
  sentinel:
    enabled: true
    datasource:
      ds:
        - nacos:
            server-addr: 192.168.195.135:8848
            groupId: TSTSE-SERVER
            dataId: sentinel-datasource
            namespace: 5aad18f7-8ffe-4538-b7e7-3e8281a403f4  # Sentinel配置的命名空间ID
            rule-type: flow


feign:
  sentinel:
    enabled: true  # 启用Feign的Sentinel支持

mybatis-plus:
  type-aliases-package: com.hzh.common.pojo
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true

pattern:
  name: 本地环境local
